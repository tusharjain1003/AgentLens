# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
asyncpg==0.29.0

# HTTP client (Jina Reader extraction + Tavily fallback)
httpx==0.27.2

# Config
python-dotenv==1.0.1
pydantic-settings==2.5.2

# ML / retrieval
sentence-transformers==3.2.1
rank-bm25==0.2.2
numpy>=1.26.0,<2.0

# LLM clients (OpenAI-compatible SDK works for DeepSeek too)
openai==1.54.4

# Web search
tavily-python==0.5.0

# HTML → text fallback extractor (when Jina Reader is blocked)
trafilatura==1.12.2

# LangGraph pipeline orchestration + LangSmith tracing
langgraph==1.1.9
langsmith==0.8.3
